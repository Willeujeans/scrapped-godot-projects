shader_type spatial;
//we want the edges to have little circles that get smaller as they reach the center
//The shading is dependent on the neighbors of this block

//Top should be a texture, of whichever material it is

void fragment() {
	ALPHA = 0.0;
}
